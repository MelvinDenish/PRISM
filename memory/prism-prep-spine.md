---
name: prism-prep-spine
description: P6 prep spine (PrepProfile + SkillSignal + readiness) is BUILT and verified; how it works and what phases remain
metadata: 
  node_type: memory
  type: project
  originSessionId: 89ed4935-5186-445c-87fa-2749d7895b91
---

PRISM's target-first architecture overhaul: spec at `docs/superpowers/specs/2026-06-12-prepare-architecture-design.md`, phased P6-P11 on top of the 5-phase Copilot plan (`~/.claude/plans/it-just-says-that-vectorized-quill.md`).

**P6 (prep spine) is DONE** (2026-06-12, 12 commits `72cd0d6..c87b02c` on `wip/pre-m1-working-changes`, subagent-driven, two-stage-reviewed, live-verified):
- `server/models/SkillSignal.js` (append-only evidence log; PILLARS = aptitude|dsa|cs_core|communication|resume) + `server/models/PrepProfile.js` (derived readiness cache, dailyPlan).
- `server/agent/services/signals.js` — `emit()` (best-effort, never throws, atomic `$set`), `readiness()` (decay-weighted mean, half-life 14d, horizon 90d), `ensureProfile()`. NO LLM in scoring.
- `server/agent/services/dailyPlan.js` — rule-based, ≤4 items. `server/routes/prepProfile.js` → `/api/prep-profile` (mentee-gated).
- Emits wired into: interviewGame submit-round, codingQuestions submit, aiInterview evaluate (persist===true only — prevents double-count with game), review grade, resumeAnalysis create, auth onboarding (idempotent diagnostic seed, 4 pillars NOT resume).
- Verify: `MONGODB_URI="mongodb://127.0.0.1:27017/prism" node seeds/verifyPrepSpine.js` (11 assertions; wipes+reseeds that mentee's signals).

**Status 2026-06-13:** Copilot P1 (persistent chat, 33f89d3..658900a), P2 (artifacts + run_code, 5e61e84..f6c412d), P3 (resume canvas, ce2892b+32ecc4c) BUILT. **P3 reviewed + live-verified inline** (subagent dispatch had died on session limit; done inline instead) — `server/seeds/verifyP3ResumeCanvas.js` = 21/21 checks (server-side changedSections diff, no-invention guardrail → 422 draft-unchanged, malformed/shape → 422, cross-user 404, real %PDF/PK export bytes, live-LLM happy path). One review-fix: export read stale `current` after async setCurrent → `saveDraft` now returns the id (commit 9d01b79). **P4 DONE (commit 18a68fc):** mentee sidebar = chat-first split — PREPARE +Review Queue, CAREER drops resume tabs, new DOCUMENTS (My Resume + Files/`/artifacts`), Resume Analysis unlinked (route kept, folded into Copilot's analyze_resume); new `pages/Artifacts.jsx`; swept untracked `ChatMessage.jsx` (committed Assistant imported it — was an integrity gap) + `ReviewQueue.jsx`. Client build passes. **P5 DONE (commit f5175e2) — Copilot plan P1–P5 COMPLETE:** per-user theming — `User.theme`(bounded accent/gradient tokens)+`greeting`, `utils/themeGenerator.js` (contrast-safe palette from a hue), register assigns randomTheme()/login+/me backfill [**REVERSED 2026-06-13: the random per-user theme auto-assignment was removed from all 3 spots in routes/auth.js — it was clobbering the gold/orange brand on every login. The app is now brand-led by default (index.css gold #C9A24B / orange #E2682A on charcoal); `apply_theme` stays as an opt-in recolor. Existing users' stored random themes need a one-time `users.updateMany({},{$unset:{theme:''}})` to fully clear.**], `apply_theme` tool+executor (executor re-derives colors from the numeric hue → injection-proof), client `utils/theme.js` inline-var apply + AuthContext re-apply on user change + index.html pre-paint + Assistant greeting. Verified `seeds/verifyP5Theme.js` + `seeds/verifyReaderGuard.js`. **Also fixed (commit 826daf9): Topics in-app reader** dumped cp-algorithms.com's news/changelog — added an article-quality guard (rejects site-root/short/link-dominated extractions → client "Open original" fallback). **Remaining: P7-11** (each needs its own writing-plans pass first; P7 needs P2/P3 — both done; P11 last). Full handoff: `~/.claude/session-data/2026-06-12-prism-copilot-p1to3-session.tmp`. Subagents: opus only (user instruction), though P3 review + P4 were done inline (cheaper, full context, and subagent dispatch kept dying on session limits).
**Integrity debt RESOLVED (commit `4cbed9e`):** `server/agent/llm.js` + `agent/services/{learningPath,mentorship,read}.js` are now tracked; `routes/{learningPaths,mentorship}.js` delegate to the shared services; pluggable Copilot LLM config (`LLM_API_KEY`/`LLM_BASE_URL`/`LLM_*_MODEL`, falls back to `GROQ_API_KEY`) landed in `config/env.js` + `.env.example`. (`models/ReviewItem.js` was already tracked via the P8 server commit.) All dependent routes + agent modules `require()` cleanly. Still untracked scratch: `seeds/verifyPrepProfileAPI.js`, `seeds/verifyReviewQueue.js`.

**P7 DONE** (commits `3724ffb`/`486a044`) and **P8 DONE** (server `5e46fb5`, client `c97b460`): targeted Interview Game — `utils/gameTemplates.js`, `/start` accepts `{companyFocus?,company?,role?,difficulty?}` (targeting defaults from PrepProfile, difficulty seeds from readiness when omitted), bank-first weighted sampling, `/submit-round` queues wrong answers → `ReviewItem`s (`reviewItemsAdded`) + emits per-round signals, `GET /:id/report`; client start screen has company/role/Auto-difficulty pickers + a post-game Performance Report deep-linking to `/review`. **P8 server logic runtime-VERIFIED**: `seeds/verifyTargetedGame.js` (local Docker mongo `prism-mongo` @127.0.0.1:27017) = 15/15 — templates, category-weighted bank sampling, wrong-answer→company-tagged ReviewItem dedup. (Atlas is dead but local Docker mongo works — see [[dev-db-setup]]; don't write "deferred, Atlas unreachable" when local verify is one command away.) **Still owed = browser pass** on the running client: company picker populates, Performance Report renders on a completed full game, `/review` deep-link navigates.

**P9a DONE** (2026-06-13, commits `f5a4909` server / `e026682` client / `6a99c39` verify; plan `docs/superpowers/plans/2026-06-13-p9a-solo-gd-scoring.md`): new `GDSession` model (server-computed scores, single-use + game-bound); `/group-discussion/evaluate` persists it + returns `gdSessionId` + new `GET /history`; `/interview-game/submit-round` GD round now scores from the GDSession (id-looked-up, user-owned, game-bound, single-use) — **closes the GD half of the client-trusted `aiScore` hole**. Client: GD round submits `gdSessionId`, round-result scorecard (wires the dead `gdEval`), GD list in History tab. Verified `seeds/verifyP9aGD.js` 4/4 vs local mongo (forged client score can't inflate) + build green. **Owed: browser smoke.**

**Still-open `aiScore` debt:** the **HR** round (and live AI interview) still trust `req.body.aiScore` in `/submit-round`'s final else — same server-graded-attempt fix applies, not yet done.

**Next: P9b** (live GD: WebRTC talk-time + Web Speech self-transcript + post-session AI summary on `GDRoom`) then P10/P11 — each needs its own writing-plans pass before coding. `gd_solo` signal source stays reserved for a future standalone GD page.

**How to apply:** any new scored feature = one `emit(userId, [{pillar, skill, score(0-1), source, sourceId}])` call after its primary write; add new sources to the SOURCES enum + SOURCE_WEIGHTS map first.
