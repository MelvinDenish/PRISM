node_modules/
.env
dist/
*.log

# Generated submission documents (sources are generate-docs.js / generate-submission.js)
*.docx

# Local Claude Code config (per-machine, not for sharing)
.claude/

# Runtime file-storage (local STORAGE_DRIVER) — uploaded files are not source
server/uploads/

# Playwright MCP test artifacts
.playwright-mcp/
