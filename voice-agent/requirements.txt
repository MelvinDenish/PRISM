# PRISM GD AI Panelist — pinned deps (LiveKit Agents 1.x).
# Bump deliberately; the agent API and plugin import paths can change between
# minor versions. Verify against https://docs.livekit.io/agents/ for your version.
livekit-agents[openai,silero,turn-detector]==1.5.8
livekit==1.0.14
python-dotenv==1.0.1
